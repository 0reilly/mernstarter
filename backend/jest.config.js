module.exports = {
  testEnvironment: 'node',
  testTimeout: 60000, // Set global timeout to 60 seconds
  forceExit: true, // Force Jest to exit after all tests are complete
  detectOpenHandles: true, // Detect open handles that are not properly closed
};

