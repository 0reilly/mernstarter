import { Email } from '@mui/icons-material';

const AppIcon = () => (
  <div className="w-12 h-12 bg-black rounded-lg flex items-center justify-center">
    <Email className="text-white" style={{ fontSize: 32 }} />
  </div>
);

export default AppIcon;